# Ensure JUnit does not run tests in parallel by default
junit.jupiter.execution.parallel.enabled=false
junit.jupiter.execution.parallel.mode.default=single
junit.jupiter.execution.parallel.mode.classes.default=single
